apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agent-marketplace

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - postgres.yaml
  - api-deployment.yaml
  # Run migrate-job.yaml manually after postgres is ready

images:
  - name: agent-marketplace-api
    newTag: latest
